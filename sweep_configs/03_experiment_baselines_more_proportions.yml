program: icu-benchmarks
command:
  - ${env}
  - ${program}
  - "train"
  - "-d"
  - "../data/miiv"
  - "-t"
  - "DatasetImputation"
  - "-c"
  - "--wandb-sweep"
  - "--wandb"
method: grid
name: baselines more proportions
parameters:
  model:
    values: ["Mean", "Median", "Zero", "MostFrequent", "MICE", "GAIN", "BRITS", "MissForest", "MLP", "RNN", "Simple_Diffusion", "CSDI", "NP", "BRNN", "SSSDS4", "Attention", "SAITS"]
  ImputationDataset.mask_method:
    values: ["MCAR", "MAR", "MNAR", "BO"]
  seed:
    values: [2222]
  ImputationDataset.mask_proportion:
    values: [0.3, 0.5, 0.7]
  data_dir:
    values: ["../data/miiv", "../data/eicu", "../data/hirid"]
