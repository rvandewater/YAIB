import icu_benchmarks.data.loader

vars = {
    "GROUP": "stay_id",
    "SEQUENCE": "time",
    "SEX": "sex",
    "DYNAMIC": ["dbp", "hr", "map", "o2sat", "resp", "sbp", "temp"],
    "STATIC": ["age", "sex", "height", "weight"],
}

preprocess.data_dir = "../data/mortality_seq/mimic"
preprocess.vars = %vars

splits.vars = %vars

Dataset.vars = %vars
