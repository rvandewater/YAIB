# Hyperparameter tuner settings for Deep Learning.
tune_hyperparameters.scopes = ["model", "optimizer"]
tune_hyperparameters.n_initial_points = 1
tune_hyperparameters.n_calls = 4
tune_hyperparameters.folds_to_tune_on = 1