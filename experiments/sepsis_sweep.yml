command:
  - ${env}
  - ${program}
  - train
  - -d
  - ../../data/
  - -t
  - BinaryClassification
  - --log-dir
  - ../yaib_logs/sepsis_sweep
  - --tune
  - --wandb-sweep
  - --wandb
  - -gc
  - -lc
method: grid
name: sepsis_sweep
parameters:
  data_dir:
    values:
      - ../../data/sepsis_calvert/miiv
      - ../../data/sepsis_moor/miiv
#      - ../data/eicu
#      - ../data/hirid
  model:
    values:
      - GRU
#      - LGBMClassifier
#      - LogisticRegression
#      - LGBMClassifier
  seed:
    values:
      - 1111
  use_pretrained_imputation:
    values:
      - None
program: icu-benchmarks